---
const links = [
  { text: "Writing", path: "/" },
  { text: "About", path: "/about" },
];
---

<nav class="absolute top-1 right-4 md:right-1/4">
  <ul class="list-none m-0">
    {
      links.map((link) => (
        <li class="inline p-0">
          <a
            href={link.path}
            class={`pr-4 lowercase ${
              Astro.url.pathname === link.path ? "font-bold" : ""
            }`}
          >
            {link.text}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
